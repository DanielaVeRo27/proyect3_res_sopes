apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: weather-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /api/tweets
        pathType: Prefix
        backend:
          service:
            name: api-rust-service
            port:
              number: 8080
